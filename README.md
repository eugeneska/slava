# CleanPro - Система управления клининговой компанией

Веб-приложение для управления клиентами, объектами, сотрудниками, услугами и расписанием.

## 🚀 Быстрый старт

### 1. Клонирование проекта
```bash
git clone <repository-url>
cd slava
```

### 2. Создание виртуального окружения

**На macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**На Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных

Отредактируйте настройки подключения к БД в `app.py`:
```python
db_config = {
    'host': '127.0.0.1',
    'user': 'root',
    'password': 'ваш_пароль',
    'database': 'cleaning_company',
    'charset': 'utf8mb4'
}
```

### 5. Запуск приложения
```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5001`

---

## 🔧 Настройка PyCharm

Если PyCharm не видит установленные пакеты, выполните следующие шаги:

### Вариант 1: Использование существующего виртуального окружения

1. **Откройте проект в PyCharm**
   - File → Open → выберите папку проекта

2. **Настройте интерпретатор:**
   - File → Settings (или `Ctrl+Alt+S` / `Cmd+,`)
   - Project → Python Interpreter
   - Нажмите на шестеренку ⚙️ → Add...
   - Выберите "Existing environment"
   - Укажите путь к интерпретатору:
     - **macOS/Linux:** `venv/bin/python`
     - **Windows:** `venv\Scripts\python.exe`
   - Нажмите OK

3. **Установите зависимости:**
   - В терминале PyCharm выполните:
   ```bash
   pip install -r requirements.txt
   ```

### Вариант 2: Создание нового виртуального окружения в PyCharm

1. **File → Settings → Project → Python Interpreter**
2. Нажмите на шестеренку ⚙️ → Add...
3. Выберите "New environment"
4. Укажите расположение: `venv` (в корне проекта)
5. Выберите базовый интерпретатор Python
6. Нажмите OK
7. **Установите зависимости:**
   - В терминале PyCharm выполните:
   ```bash
   pip install -r requirements.txt
   ```

### Проверка настройки

После настройки интерпретатора:
- В PyCharm должен исчезнуть красный подчеркивание у импортов (`from flask import ...`)
- В терминале PyCharm должна быть активирована виртуальная среда (видно `(venv)` в начале строки)

---

## ⚠️ Решение проблем

### Проблема: "Flask уже установлен, но PyCharm его не видит"

**Причина:** Flask установлен в другом окружении (глобально или в другом venv), а PyCharm использует другое окружение.

**Решение:**
1. Убедитесь, что виртуальное окружение создано в папке проекта
2. Настройте PyCharm на использование этого окружения (см. выше)
3. Установите зависимости заново в правильном окружении:
   ```bash
   # Деактивируйте текущее окружение (если активно)
   deactivate
   
   # Активируйте venv проекта
   source venv/bin/activate  # macOS/Linux
   # или
   venv\Scripts\activate  # Windows
   
   # Установите зависимости
   pip install -r requirements.txt
   ```

### Проблема: "ModuleNotFoundError: No module named 'flask'"

**Решение:**
1. Проверьте, что виртуальное окружение активировано (в терминале видно `(venv)`)
2. Установите зависимости: `pip install -r requirements.txt`
3. Проверьте настройки интерпретатора в PyCharm

---

## 📋 Зависимости

- Flask==3.0.0
- mysql-connector-python==8.2.0
- Werkzeug==3.0.1

---

## 📝 Структура проекта

```
slava/
├── app.py                 # Основной файл приложения
├── requirements.txt       # Зависимости проекта
├── templates/            # HTML шаблоны
│   ├── base.html
│   ├── dashboard.html
│   ├── login.html
│   └── ...
└── README.md
```

---

## 🔐 Первый вход

После первого запуска создайте учетную запись администратора через страницу регистрации.

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Правильность настроек базы данных
2. Активность виртуального окружения
3. Установлены ли все зависимости из `requirements.txt`
4. Настроен ли правильный интерпретатор в PyCharm
